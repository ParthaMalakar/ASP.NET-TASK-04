@model IEnumerable<WebApplication7.DB.Cours>

<form action="" method="post">
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PreReq)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PreReq)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>
            </tr>
        }
            <tr>
               
                <td>
                    <input class="h-16 pl-2 w-full" placeholder="Enter course name" name="Name" /><br />
                    <span class="text-sm text-red-500">
                     
                    </span>
                </td>
                <td>
                    <input class="h-16 pl-2 w-full" placeholder="Enter pre-requisite id" name="PreReq" /><br />
                    <div class="text-sm text-red-500">
                       
                    </div>
                </td>
                <td class="px-4 font-semibold"><button class="text-white w-full py-3 px-4 bg-[#00b843] rounded-lg shadow-xl">ADD</button></td>
            </tr>
        

    </table>
    </form>